import React from 'react'
import './Loader.scss'

const Loader = () => (
    <div className="loader">
        <span className="loader__spinner"></span>
        <p>Loading...</p>
    </div>
)

export default Loader
